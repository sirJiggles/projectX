import React from 'react';
import { TabBarIOS, Platform, TabBarIOSItem, Text, View } from 'react-native';

export default function TabBar() {
  // const render =
  //   Platform.OS === 'ios' ? 'thing' : <div>Unsupported platform</div>;
  return (
    <View>
      {/* 👇this does not exist any more :/ still in the docs though */}
      {/* <TabBarIOS barTintColor="#000"></TabBarIOS> */}
    </View>
  );
}
